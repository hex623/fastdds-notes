# A2 - 核心类速查表

**整理时间**: 2026-03-17

---

## 类继承关系图

```
eprosima::fastdds::dds::DomainEntity
├── DomainParticipant
├── Publisher
├── DataWriter
├── Subscriber
└── DataReader

eprosima::fastdds::rtps::RTPSDomain (单例)
├── RTPSParticipant
│   ├── RTPSWriter ──→ WriterHistory
│   └── RTPSReader ──→ ReaderHistory

Endpoint (抽象基类)
├── RTPSWriter
│   ├── StatelessWriter
│   └── StatefulWriter
└── RTPSReader
    ├── StatelessReader
    └── StatefulReader
```

---

## 核心类速查

### RTPSDomain
| 方法 | 作用 |
|------|------|
| `createParticipant()` | 创建 Participant |
| `removeParticipant()` | 移除 Participant |
| `createRTPSWriter()` | 创建 Writer |
| `createRTPSReader()` | 创建 Reader |

### RTPSParticipant
| 方法 | 作用 |
|------|------|
| `getGuid()` | 获取 GUID |
| `newWriter()` | 创建 Writer |
| `newReader()` | 创建 Reader |
| `getRTPSParticipantAttributes()` | 获取配置 |

### RTPSWriter
| 方法 | 作用 |
|------|------|
| `write()` | 发送数据 |
| `new_change()` | 创建 CacheChange |
| `send_to_all_readers()` | 发送到所有 Reader |
| `update_reader_info()` | 更新 Reader 信息 |

### RTPSReader
| 方法 | 作用 |
|------|------|
| `process_data()` | 处理接收数据 |
| `add_change()` | 添加到 History |
| `send_acknack()` | 发送确认 |
| `matched_writer_add()` | 添加匹配的 Writer |

---

## 关键数据结构

### GUID
```cpp
struct GUID_t {
    GuidPrefix_t guidPrefix;  // 12 bytes
    EntityId_t entityId;      // 4 bytes
};

// 示例: 01.0f.da.70.f5.ea.d7.9b.00.00.00.00|0.0.1.4
//       └──────── GUID Prefix ────────┘ |Entity|
```

#### GUID Prefix 组成 (12 bytes)
| 字段 | 大小 | 说明 |
|------|------|------|
| Vendor ID | 2 bytes | 厂商标识 (0x010f = eProsima) |
| Domain ID | 2 bytes | 域标识 |
| Instance ID | 8 bytes | MAC/时间戳/随机数 |

#### Entity ID 组成 (4 bytes)
| 字段 | 大小 | 说明 |
|------|------|------|
| Entity Type | 3 bytes | 实体类型 (Participant/Writer/Reader) |
| Instance Num | 1 byte | 实例编号 |

#### 唯一性保证
- MAC + 时间戳 + 随机数 + 冲突检测
- 即使同一台机器同时启动多个 Participant 也能区分

### CacheChange
```cpp
struct CacheChange_t {
    ChangeKind_t kind;              // ALIVE, DISPOSE, etc.
    GUID_t writerGUID;              // 写入者 GUID
    SequenceNumber_t sequenceNumber; // 序列号
    SerializedPayload_t serializedPayload; // 序列化数据
    Time_t sourceTimestamp;         // 时间戳
    InstanceHandle_t instanceHandle; // 实例句柄
};
```

### SequenceNumber
```cpp
struct SequenceNumber_t {
    int32_t high;   // 高32位
    uint32_t low;   // 低32位
};
// 支持 2^64 个序列号，实际使用 low 部分
```

---

## QoS 策略速查

| QoS | Writer | Reader | 作用 |
|-----|--------|--------|------|
| **RELIABILITY** | ✅ | ✅ | 可靠/BEST_EFFORT |
| **DURABILITY** | ✅ | ✅ | 历史数据保留 |
| **HISTORY** | ✅ | ✅ | 缓存深度 |
| **LIVELINESS** | ✅ | ✅ | 活性检测 |
| **DEADLINE** | ✅ | ✅ | 发布周期 |
| **LIFESPAN** | ✅ | ✅ | 数据有效期 |
| **OWNERSHIP** | ✅ | ✅ | 所有权策略 |

---

## RTPS 消息类型

| 消息 | 方向 | 作用 |
|------|------|------|
| **DATA** | W→R | 传输用户数据 |
| **DATA_FRAG** | W→R | 分片大数据 |
| **HEARTBEAT** | W→R | 确认请求 |
| **ACKNACK** | R→W | 确认/重传请求 |
| **GAP** | W→R | 数据间隙标记 |
| **INFO_TS** | W→R | 时间戳信息 |
| **INFO_DST** | R→W | 目标信息 |
| **INFO_SRC** | W→R | 源信息 |

---

## 文件位置速查

| 组件 | 路径 |
|------|------|
| RTPS 层 | `include/fastdds/rtps/` |
| DDS 层 | `include/fastdds/dds/` |
| Writer | `src/cpp/rtps/writer/` |
| Reader | `src/cpp/rtps/reader/` |
| History | `src/cpp/rtps/history/` |
| 流量控制 | `src/cpp/rtps/flowcontrol/` |
| 传输层 | `src/cpp/transport/` |
| 安全 | `src/cpp/security/` |

---

## 常见端口号

| 端口 | 用途 |
|------|------|
| **7400** | 默认 RTPS 端口 (SPDP) |
| **7410** | 用户流量 (Participant 0) |
| **7411** | 用户流量 (Participant 1) |
| **11811** | Discovery Server 默认端口 |

---

_整理自 2026-02-27 ~ 2026-03-05 学习笔记_
